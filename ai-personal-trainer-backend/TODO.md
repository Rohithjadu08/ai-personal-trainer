# ai-personal-trainer-backend - TODO

- [x] Fix register email collision by ensuring a single in-memory DB instance is used consistently.
- [ ] Add debug endpoint to return current in-memory user count and check stored emails (optional but useful).
- [ ] Restart backend server and verify fresh registration works with a new email.

